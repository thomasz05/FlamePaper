From 312f8e6a843343ac4812e9be298c6d598630e8da Mon Sep 17 00:00:00 2001
From: LinsaFTW <25271111+linsaftw@users.noreply.github.com>
Date: Sun, 13 Mar 2022 22:56:52 -0300
Subject: [PATCH] Disable Unloaded Chunk Movement


diff --git a/src/main/java/net/minecraft/server/Entity.java b/src/main/java/net/minecraft/server/Entity.java
index 2da303fee..029eee7c6 100644
--- a/src/main/java/net/minecraft/server/Entity.java
+++ b/src/main/java/net/minecraft/server/Entity.java
@@ -432,6 +432,11 @@ public abstract class Entity implements ICommandListener {
 
     public void move(double d0, double d1, double d2) {
         if (this.loadChunks) loadChunks(); // PaperSpigot - Load chunks
+        // FlamePaper start - Disable Unloaded Chunk Movement
+        if (!((ChunkProviderServer) world.chunkProvider).getChunkAt((int) d0 >> 4, (int) d2 >> 4).isDone()) {
+            return;
+        }
+        // FlamePaper end - Disable Unloaded Chunk Movement
         if (this.noclip) {
             this.a(this.getBoundingBox().c(d0, d1, d2));
             this.recalcPosition();
-- 
2.32.0

